loalang.xyz {
    tls {
        dns cloudflare
    }

    gzip

    header / -Server

    expires {
        match .css|.js$ 1y
    }

    proxy / www
}

api.loalang.xyz {
    tls {
        dns cloudflare
    }

    gzip

    header / -Server

    proxy / api
}
